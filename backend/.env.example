# =============================================
#  ENTRAIDE ACADÉMIQUE — Variables d'environnement
#  Copier ce fichier en .env et remplir les valeurs
# =============================================

# Base de données Render (déjà déployée)
DATABASE_URL=postgresql://chris:VOTRE_MOT_DE_PASSE@dpg-d7jurbbeo5us73cvovng-a.frankfurt-postgres.render.com/entraide?sslmode=require

# JWT — Générer avec : node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=remplacez_par_une_chaine_aleatoire_tres_longue_ici
JWT_EXPIRES_IN=7d

# Cloudinary — https://cloudinary.com (compte gratuit)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Application
PORT=5000
CLIENT_URL=http://localhost:5173
NODE_ENV=development
